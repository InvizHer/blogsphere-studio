# Cloudflare Pages configuration (optional — Pages auto-detects Vite)
# This file is only needed if you use `wrangler pages deploy`
name = "inkwell-blog"
compatibility_date = "2024-09-23"

[site]
bucket = "./dist"
